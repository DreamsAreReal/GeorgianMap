namespace GeorgiaPlaces.Domain;

/// <summary>
/// Marker for assembly scanning. Domain layer has no dependencies on
/// frameworks — pure C# entities, value objects, and domain events.
/// </summary>
public sealed class AssemblyMarker;
